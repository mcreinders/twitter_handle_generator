/**
 * Created by user on 1/11/16.
 */

var express = require('express');
//var adjectives = require('./server/public/json_data/adjectives.json');
//var nouns = require('./server/public/json_data/nouns.json');

var router = express.Router();

//router.get('/data', function(request, response){
//    var returnResponse = "this is a test";
//    response.send(returnResponse);
//});

//use this to shuffle arrays
//function shuffleArray(array) {
//    for (var i = array.length - 1; i > 0; i--) {
//        var j = Math.floor(Math.random() * (i + 1));
//        var temp = array[i];
//        array[i] = array[j];
//        array[j] = temp;
//    }
//    return array;

module.exports = router;